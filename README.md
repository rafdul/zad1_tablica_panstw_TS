# zad1 tablica panstw (typescript)

*npm init*

Project with parcel-bundler. 
Parcel has a development server built in, which will automatically rebuild your app as you change files:

*npx parcel index.html*
